# 2026-05-09

## Session 1 — full day (CST)

First v0.0.3 implementation session. Three things landed (T0.1, Epic 5
PR-A, T5.1) with one mid-session pivot (cost analysis → new epic) and
one verification-driven re-open (T5.1 verbatim contract).

**Done**

- **T0.1 (#19) — `maestro/paths.py` + bootstrap shim**. First closed
  loop of v0.0.3. 7 path functions per ADR-0003, 10 unit tests, no I/O,
  no side effects. Bootstrap MCP server gains a transitional sys.path
  shim so the new package layout is exercised on startup. First
  dogfooding closed loop: paths + tests dispatched to `cheap_code_gen`
  (deepseek-coder), worker produced both files in 22s / 3366 tokens;
  one minor docstring fix on integration. Branch
  `feature/19-paths` → `--no-ff` merged into `v0.0.3` →
  `9f86049` / `e272ed0`. #19 closed with completion comment.

- **Epic 5 (#52) PR-A — design doc + ADR-0008 + sub-issues**. Mid-session
  cost analysis (see Decided / Process learnings) led to spinning up a
  new epic mid-implementation. PR-A landed:
  - `docs/design/52-epic5-worker-fleet-expansion.md` Status: approved
  - `docs/adr/0008-worker-role-naming-and-io-conventions.md` Status: accepted
  - Sub-issues #53 (T5.1) and #54 (T5.2) created and linked to #52
  - Parent #52 body updated with sub-issue references
  - Branch `feature/52-worker-fleet-design` → `--no-ff` merged into
    `v0.0.3` → `8954222` / `c852ba9`.

- **T5.1 (#53) — librarian + rename `cheap_code_gen → coder` + model
  bump**. Two-phase landing:
  - **Phase 1** (`9fb04e1` / `428fa9e`): librarian role implemented in
    `bootstrap/maestro_server.py` — file-reading worker, structured JSON
    output with verbatim-contract `hard_constraints`. Refactored the
    server to a `TOOLS_REGISTRY` dict (adding T5.2's reviewer + scribe
    will be one entry each). Renamed `cheap_code_gen → coder` across
    11 forward-looking files; 6 historical-fact references preserved
    per design §3.6 rule. Resolved OPEN-5.6 by bumping `coder` from
    `deepseek-coder` to `deepseek-v4-pro`. 26 new unit tests.
    Second dogfooding loop: librarian core dispatched to
    `cheap_code_gen` worker, 28s / 4470 tokens, ~150 lines of correct
    code, 1 minor integration cleanup.
  - **Phase 2** (`d91fda7` / `3e08315`) — verbatim contract hardening.
    Real-world verification (calling librarian against
    `docs/design/13-epic1-team-composition.md`) found 4/8 quotes
    failing verbatim (50% violation rate): v4-flash strips markdown
    emphasis (`**X**` → `X`) and occasionally paraphrases while
    quoting. Three-part fix: (A) `_verify_verbatim_quotes` server-side
    check after `_validate_librarian_output`; non-verbatim entries
    dropped + reported in `concerns`; (B) tightened
    `LIBRARIAN_SYSTEM_PROMPT` with explicit anti-examples; (D) ADR-0008
    § I/O contract pattern gained a `Verbatim verification` paragraph
    generalizing the pattern. 7 new tests. Re-verification: **4/4
    surviving quotes are verbatim (100%)**. v4-flash produces ~40% hit
    rate on attempted quotes (60% dropped) but the contract now holds
    absolutely; OPEN-5.7 tracks the hit rate.
  - #53 closed after Phase 2 verification.

- **Memory baselines added to user memory**:
  - `feedback_code_quality_baseline.md` — business-perspective comments
    on core logic, single responsibility, unit tests required for every
    Maestro implementation task.
  - `feedback_dogfooding_implementation.md` — implementation phase must
    dispatch to workers when feasible; surface dogfooding-blockers and
    worker-quality issues, don't silently fall back to orchestrator.

**Decided**

- **Worker fleet expansion is v0.0.3 scope, not deferred**. Epic 5
  (worker fleet expansion: librarian, reviewer, scribe + rename) folded
  into v0.0.3 mid-implementation rather than pushed to v0.0.4. Rationale:
  the librarian role pays for itself across the remaining 32 v0.0.3
  implementation tasks (each begins with whole-doc reading). The
  meta-pattern: dogfooding's first task surfaced a real cost problem;
  the right response is to build the role that solves it now, not after
  v0.0.3 ships.

- **Role naming convention** (ADR-0008): team-member-style nouns
  (`coder`, `librarian`, `reviewer`, `scribe`); no `cheap_` prefix; no
  model in the name; single token preferred. The fleet reads as a team
  roster, not a function library.

- **I/O contract conventions** (ADR-0008): structured input + output
  per role; every output carries a `concerns` channel for worker
  self-doubt; output validators run before return; verbatim-contract
  fields are server-side-verified, not just system-prompt-trusted.

- **Worker file access** (ADR-0008 + librarian implementation): workers
  MAY read files directly when input would otherwise force the
  orchestrator to load it into its expensive context. Load-bearing for
  librarian's economics — without this principle, the role would
  defeat its own purpose.

- **Shadow mode rollout**: workflow contract recorded in user memory
  (`feedback_shadow_mode_active.md`, to be created in T5.2). Membership
  in the file determines whether the orchestrator runs its own version
  in parallel and presents both to the user. Promotion is by explicit
  user signal; no numeric threshold.

- **`coder` model bump to `deepseek-v4-pro`** (resolves OPEN-5.6).
  Rationale: keeping coder on the legacy `deepseek-coder` model would
  force every future prompt-tuning decision to track two model
  lineages. The v4 lineup is the project's forward direction.
  `MODEL_PRO` and `MODEL_FLASH` constants live in
  `bootstrap/maestro_server.py`, env-overridable via
  `MAESTRO_MODEL_PRO` / `MAESTRO_MODEL_FLASH`.

- **Verification > unit tests for contract checking**. Phase 1 of T5.1
  passed 26 unit tests (mocked client) but failed real-world contract
  verification 50%. Mocked tests cannot exercise the actual model's
  fidelity to a verbatim contract — only a real call can. Lesson
  recorded: any future role with a contract whose enforcement depends
  on model behavior MUST include a real-world smoke step in its
  acceptance criteria, not just unit tests.

- **Tasks close when verification passes, not when code lands**. The
  user explicitly noted: "我认为任务完成了，就可以关闭相关的任务了"
  but the corollary is verification before closing. T5.1's first close
  was premature (before real-world verification); reopening was the
  right call.

**Deferred**

- **T5.2 (#54)** — reviewer + scribe + shadow-mode memory feedback.
  Next Epic 5 task. Not blocking — Epic 0 / 1 / 2 / 3 implementation
  can proceed in parallel and use librarian + coder as needed.
- **Librarian quote hit rate** (OPEN-5.7) — observe across multiple
  uses before deciding remediation path (further prompt tightening /
  switch to v4-pro / adjust verifier normalization).
- **Real `mcp__maestro__librarian` invocation through Claude Code's
  tool list** — current session's MCP server was the v0.0.2 build (only
  `cheap_code_gen` exposed). Verifying via importlib + real DeepSeek
  API is functionally equivalent for contract testing, but the next
  session will exercise the librarian through Claude Code's tool
  discovery once the user restarts and the MCP server re-registers.

**Handoff for next session**

- **Branch state**: on local `v0.0.3`, ahead of `origin/main` by 41
  commits, fully pushed to `origin/v0.0.3`. Working tree clean.
- **Open issues at session end**: tracking (#2, #3); v0.0.3 epics
  (#11–#16, #52); v0.0.3 sub-issues (#20–#51, #54). Closed today: #19
  (T0.1), #53 (T5.1).
- **Next implementation candidates** (pick one):
  1. **T5.2 (#54)** — reviewer + scribe + shadow-mode memory. Finishes
     Epic 5; unblocks shadow-mode rollout for code review and commit
     drafting in subsequent tasks.
  2. **T0.2 (#20)** — extend `.env` loader to also check
     `~/.maestro/credentials.env`. Smallest Epic 0 task; first real
     use of librarian to read Epic 0 design + ADR-0003.
- **First action in next session — MCP-path validation of librarian**.
  Session 1 verified librarian's contract via importlib + real DeepSeek
  API, but the current Claude Code session is still bound to the
  pre-T5.1 MCP server (only `cheap_code_gen` exposed). After the user
  restarts Claude Code, the next session's tool list should include
  `mcp__maestro__librarian` and `mcp__maestro__coder` (no longer
  `cheap_code_gen`). Before any other work, the next session MUST:
    1. Confirm both new tools appear in the deferred-tools list at
       session start (proves MCP discovery picked up the new server).
    2. Make one real call: `mcp__maestro__librarian(file_path="docs/design/13-epic1-team-composition.md", query="...")`
       — same query as Session 1's importlib test if possible, for an
       apples-to-apples comparison.
    3. Verify the returned `hard_constraints[*].quote` are all verbatim
       (orchestrator spot-check). Verify `concerns` includes a
       `librarian self-validation dropped N` line if the verifier
       fired. **Only with this confirmation is T5.1 truly closed
       end-to-end.**
- **Mental thread for next session**: with librarian validated through
  the MCP path, the Implementation-start protocol's "open every link in
  Design references" step should dispatch to librarian rather than
  read whole files into Opus context. Test this protocol shift in the
  first real task of the next session — that's the loop closure on
  today's cost analysis.
- **Watchpoints carried forward**:
  - **OPEN-5.7** librarian hit rate — collect data across 5–10 real
    uses before deciding to retune.
  - **OPEN-5.1** librarian on > 80K-char documents — handler refuses;
    revisit on first observed refusal.
  - The `cheap_code_gen` worker (Phase 1 of T5.1 dispatched to it)
    will go away once the user restarts Claude Code; subsequent
    dispatches will go to `coder` on `deepseek-v4-pro`. Watch the
    first few `coder` calls for behavior changes due to the model
    bump.
  - The bootstrap `sys.path` shim (T0.1) is transitional; T0.5 will
    replace with `pyproject.toml` packaging.

**Process learnings**

- **Cost analysis surfaces product, not just optimization**. Mid-session
  the user asked why today's token spend was high. The honest answer
  ("reading whole design docs, plus protocol overhead, plus per-turn
  fixed cost") could have ended at "let me write tighter responses."
  Instead it became: which of these workloads can a cheap worker
  do? — which became Epic 5. The same principle the project is
  built on (dispatch execution-heavy work to cheap models) applies to
  the orchestrator's own work, recursively. This is the project
  finding itself.

- **Real-world verification is not optional for contract roles**. T5.1's
  first close was wrong: 26 unit tests passed but the verbatim contract
  failed 50% on first real use. The lesson is structural — any role
  whose contract depends on model behavior (verbatim, JSON-schema
  adherence, formatting) cannot be unit-tested into compliance.
  Acceptance criteria must include a real call.

- **The verifier is the contract; the prompt is the expectation**.
  When the librarian system prompt was tightened with explicit
  anti-examples, v4-flash *still* produced 60% non-verbatim quotes
  on prose passages. The system prompt influences the worker; it
  doesn't bind it. Code-level enforcement does. ADR-0008 now codifies
  this: any field with a verbatim contract MUST have its handler
  verify before returning.

- **Re-opening is the right move when verification fails**. The user's
  framing: closing requires verification, not just code-landing. The
  flow `close → verify → reopen on failure → fix → re-verify → close`
  is honest about what "done" means. Worth more than a CI-gating
  ceremony.

- **AI-fleet expansion from observed costs > AI-fleet expansion from
  speculation**. Epic 5's three roles weren't picked off a feature
  list; each was traced to a specific consumption pattern in T0.1's
  session log. `librarian` ↔ "reading whole design docs"; `reviewer`
  ↔ "evaluating worker code line-by-line"; `scribe` ↔ "drafting commit
  messages from a known diff." The discipline: only build a role when
  its consumption pattern has been observed with the orchestrator's
  own time. Otherwise the role's value is hypothetical.

- **Documentation cost was again decisive on flexibility**. Mid-session
  decisions (Epic 5 introduction; coder model bump; verbatim
  hardening) all required updating documents that had already been
  written and approved. Each change cost minutes because the docs
  exist; in a code-only world the same change would have required
  retracing through scattered implicit knowledge. The
  documentation-as-throughput-multiplier reframe (yesterday's project
  memory entry) keeps holding empirically.

---

## Session 2 — late afternoon → evening (CST)

After Session 1 wrote the (premature) journal, work continued through
the rest of the v0.0.3 implementation day: T5.1 final polish, T5.2 land,
T5.3 design + implementation + retro, and end-to-end MCP-path validation
after a Claude Code restart. Net: Epic 5 fully shipped, 5 worker tools
on the live MCP path, async dispatch infrastructure removes the
~60s MCP timeout as a ceiling on dogfooding.

**Done**

- **T5.1 (#53) Phase 3 — final polish, semantic-verbatim recalibration**
  (`605fb9e` / `cfe897c`). Two findings from MCP-path testing folded
  back: (1) relative `file_path` resolves against `_PROJECT_ROOT` in
  the handler so callers can pass repo-relative paths despite the MCP
  server's CWD being elsewhere; (2) verbatim contract on
  `hard_constraints[*].quote` recalibrated from byte-strict (with
  markdown emphasis preserved) to **semantic-verbatim** (whitespace +
  `**bold**` normalized on both sides; backticks / brackets / em-dashes
  / words still strict). The strict version dropped 60% of v4-flash's
  prose quotes while protecting against zero real word-level violations
  — wrong calibration. After recalibration: 9/9 hit rate on the same
  test query that had produced 0/9 before. ADR-0008 § Verbatim
  verification updated with the rationale; new test
  `test_verify_accepts_markdown_stripped` reverses the previous test's
  assertion.

- **T5.2 (#54) — reviewer + scribe in shadow mode**
  (`a8a4e2d` / `599b113`). Both roles implemented per design 52
  §3.3–3.5: structured I/O, system prompts, validators, handlers.
  Shadow-mode mechanism codified via `feedback_shadow_mode_active.md`
  user memory file listing both roles with `2026-05-09` start date and
  `explicit user signal` promotion criterion. 14 new unit tests passed
  (63 total at this point).

  **The dogfooding obstacle that drove T5.3**: both core implementations
  were initially attempted via `coder` dispatch on detailed ~150-line
  specs. Both calls hit Claude Code's hard-coded ~60s MCP request
  timeout (server-side processing continued, but client-side response
  was lost). The roles were therefore written directly using
  coder/librarian as templates. Recorded as a real production
  limitation worth fixing rather than papering over — became T5.3.

- **Dogfooding philosophy correction → memory update**. After T5.2
  landed, I framed the timeout-bypass as "spec was so detailed that
  worker became transcription, so writing directly was right." The
  user pushed back firmly: the right metric is "quality at lower token
  cost," and a clear spec is the **best** dispatching candidate, not a
  worse one. Worker is a cost-saving tool, not a colleague seeking
  creative space. The orchestrator is the architect/senior engineer;
  it steps in only when the worker can't cover. `feedback_dogfooding_implementation.md`
  rewritten to record this and to flag "spec is too detailed to
  bother dispatching" as an explicit anti-pattern. The implication:
  T5.2's hand-written reviewer/scribe wasn't a fine call — it was
  bypassing infrastructure debt that needed fixing.

- **T5.3 (#55) — async dispatch infrastructure**
  (`71afba1` / `6e0ec87`). Resolves the timeout ceiling with an
  enqueue + poll pattern.
  - Each worker tool returns `{job_id}` immediately, schedules the
    impl as `asyncio.create_task`, runs in background.
  - New `mcp__maestro__job_status` infrastructure tool returns
    `running` / `done` / `failed` for any job_id.
  - In-memory job dict; persistence deferred to Epic 4 per ADR-0009.
  - `result_text` string envelope (caller parses based on which tool
    was originally dispatched) — keeps role-specific shapes out of the
    transport layer.
  - Each MCP call (enqueue + each poll) returns in <1s, so the 60s
    transport ceiling no longer matters.
  - 5 new unit tests; 68 tests pass total (10 paths + 58 workers).

- **ADR-0009** — async dispatch via enqueue + poll. Records the
  pattern, the in-memory state decision, the result-text envelope,
  five rejected alternatives, consequences, and reversibility.

- **T5.2 retro-redo** (in T5.3). reviewer + scribe specs dispatched to
  `coder` via the new async path:
  - reviewer: 94s wall, completed successfully
  - scribe: 86s wall, completed successfully
  - Both would have failed at 60s under the synchronous path
  - Worker outputs saved to `/tmp/t5.2_retro_{reviewer,scribe}.txt` as
    dogfooding evidence. Hand-written implementations retained in
    tree (style-consistent with librarian/coder); worker outputs
    confirm the dispatch was achievable, not the choice of
    integration.

- **End-to-end MCP-path validation after Claude Code restart**.
  - Tool discovery: 5 tools online (`coder` / `librarian` / `reviewer`
    / `scribe` / `job_status`).
  - Real `mcp__maestro__coder` round-trip (`is_palindrome` spec):
    enqueue → job_id → poll → done at 11s; result_text contains the
    expected `[coder dispatch — deepseek-v4-pro — 11.18s — ...]`
    banner + structured response.
  - Real `mcp__maestro__librarian` round-trip (T1.1 query, repo-
    relative path): enqueue → job_id → poll → done at ~30s; 8
    hard_constraints, all 8 externally verified semantic-verbatim;
    `concerns: []` (verifier dropped nothing).
  - Confirms T5.3's OPEN-T5.3.1: the MCP transport layer also
    successfully sidesteps the 60s ceiling under the new async pattern.

**Decided**

- **Verbatim contract is word identity, not byte identity** (T5.1
  Phase 3). The orchestrator cares what the doc says, not how it
  renders. The verifier strips whitespace + `**bold**` before
  comparison; everything else (backticks, brackets, em-dashes, words,
  punctuation) is strict. ADR-0008 carries the rationale.

- **Worker dispatch is the default for code work**, not "the option
  when there's room for the worker to perform" (corrected from my
  midday wrong framing). Cost-saving tool, not a creative collaborator.
  Orchestrator steps in only when worker can't cover. Captured in
  `feedback_dogfooding_implementation.md`.

- **Infrastructure obstacles must be fixed, not bypassed**. The MCP
  timeout was a real obstacle to dogfooding; T5.3 fixed it rather than
  letting "I'll just write it directly" become precedent. The general
  rule: any time "this can't be dogfooded" surfaces, ask "is this a
  real role-capability gap or an infrastructure debt?" The latter gets
  paid down.

- **Async dispatch is the default protocol for all worker roles**
  (ADR-0009). Every role enqueues + returns job_id. Caller (orchestrator)
  polls job_status. No more synchronous worker calls in v0.0.3+.
  Generalizes for any future role.

- **Result-text string envelope at the async-transport layer**.
  Different roles emit different shapes (librarian → JSON, coder →
  formatted text); forcing a uniform parsed envelope at transport pushes
  role-specific logic into infrastructure. Keep transport dumb.

- **Job state in-memory only for v0.0.3**. Restart-during-job is rare;
  Epic 4 packaging revisits if needed. Deliberately accepting fragility
  for the simpler model.

- **Tasks close only after verification, not just after code lands**.
  Reinforced today: T5.1 was closed prematurely after Phase 1, then
  reopened after MCP testing showed the verbatim contract failing 50%.
  T5.2 was closed after unit tests, then real-world testing revealed
  the MCP timeout problem. The flow `close → verify → reopen on
  failure → fix → re-verify → close` is now project DNA.

**Deferred**

- **Reviewer / scribe promotion out of shadow mode**. Need real usage
  data: each role needs to be exercised in shadow on at least a few
  real tasks (code review during implementation; commit drafting on
  PRs) before the user is positioned to judge promotion. Membership in
  `feedback_shadow_mode_active.md` is the gate.
- **Persistent job state across server restarts** (ADR-0009). Trigger:
  observed loss of substantive work due to restart.
- **Job cancellation API**. Not needed in v0.0.3 — caller can stop
  polling; impl runs to completion.
- **Result TTL / GC**. Memory accumulation acceptable at v0.0.3 scale;
  bootstrap-tier server restarts often.
- **Multi-orchestrator coordination**. v0.0.3 is single-client.
- **First real reviewer dispatch**. Shadow-mode protocol is wired but
  no implementation task has yet exercised it. Probably first occurs in
  T0.2 or T1.1's verification step.

**Handoff for next session**

- **Branch state**: on local `v0.0.3`, fully pushed to `origin/v0.0.3`,
  47 commits ahead of `origin/main`. Working tree clean.
- **Open issues at session end**: tracking (#2, #3); v0.0.3 epics
  (#11–#16, #52); v0.0.3 sub-issues (#20–#51) for Epic 0/1/2/3.
  **Closed today**: #19 (T0.1), #53 (T5.1), #54 (T5.2), #55 (T5.3).
  Epic 5 is fully done — all four sub-issues closed; the parent (#52)
  stays open until the v0.0.3 release PR auto-closes it.
- **Next implementation candidates** (pick one):
  1. **T0.2 (#20)** — extend `.env` loader to also check
     `~/.maestro/credentials.env`. Smallest Epic 0 task. **First real
     test of the new dispatch protocol**: orchestrator should
     dispatch design-doc reading to librarian (via `mcp__maestro__librarian`
     + `mcp__maestro__job_status`), then dispatch implementation to
     coder, then run reviewer in shadow on the result, then run scribe
     in shadow for the commit message. End-to-end first usage of the
     full Epic 5 fleet.
  2. **T1.1 (#26)** — Pydantic team config models + DEFAULT_MODELS.
     Slightly larger; the librarian validation already covered the
     hard constraints needed for this task (cached in
     `/tmp/async_smoke.py` output).
- **Mental thread for next session**: the dogfooding loop is now
  closed end-to-end. Every implementation task from T0.2 forward
  should exercise the full role fleet (librarian → coder → reviewer
  shadow → scribe shadow). After 3–5 such tasks, the user will have
  enough data to promote reviewer / scribe out of shadow.
- **Watchpoints carried forward**:
  - **OPEN-5.7** (librarian hit rate) — effectively resolved by
    semantic-verbatim recalibration. Still worth observing across more
    tasks.
  - **OPEN-T5.3.1** — resolved by today's MCP-path validation.
  - **OPEN-5.1** (oversize documents) — handler refuses; revisit on
    first observed refusal.
  - **Reviewer / scribe shadow membership** — keep
    `feedback_shadow_mode_active.md` accurate; user signals
    promotion.
  - The `bootstrap` `sys.path` shim (T0.1) is still transitional;
    T0.5 replaces it with `pyproject.toml` packaging.

**Process learnings**

- **Verifier > prompt for any contract that depends on model
  behavior**. Reinforced twice today: T5.1 Phase 2 (the prompt alone
  produced 50% verbatim violations even with explicit anti-examples;
  the verifier got it to 0% bad-quote leakage) and Phase 3 (the
  verifier's calibration mattered as much as its existence — wrong
  calibration produced 0% hit rate on otherwise-correct quotes). Both
  realizations belong in ADR-0008's permanent record.

- **Mid-session framing corrections are valuable signal, not friction**.
  My "spec too detailed → ceremony" framing was a real lapse — wrong
  enough that the user had to actively refute it. Capturing that
  refutation in memory means the wrong frame doesn't recur. The cost
  was minutes; the avoided cost over the project's lifetime is much
  larger.

- **Recursive dogfooding compounds**: Maestro discovered (during T5.2)
  that its dogfooding pipeline was infrastructure-blocked, and the
  remedy (T5.3) was itself developed using Maestro's worker (the
  retro-redo dispatched the same blocked specs through the new path
  and they completed). The project finds its own cracks via use, then
  closes them via use. Today's session validates that loop in real
  time — the most important data point of v0.0.3 so far.

- **Closing prematurely is failure mode worth name-checking**. T5.1
  closed → reopened after verification revealed the verbatim
  contract failing. T5.2 closed → real-world testing immediately
  surfaced the MCP timeout. The pattern: unit tests with mocked
  clients cannot validate contracts whose enforcement depends on
  model behavior or transport-layer timing. Acceptance criteria
  must include a real-call step. Documented now in
  `feedback_dogfooding_implementation.md`.

- **The cost-analysis → product-feature pivot is repeatable**. Today
  it spawned Epic 5 and ADR-0009 from the question "where is my
  Opus token spend going?" The general form: when a cost shows up,
  ask "is this an orchestrator-only workload, or could a cheaper
  worker do it given the right scaffolding?" Often the answer is
  the latter, and the scaffolding is small. This is the working
  thesis of Maestro reduced to a single rule.

- **ADRs absorbed updates today without ceremony**. ADR-0008 gained
  two new paragraphs (Verbatim verification → its calibration); ADR-
  0009 was written fresh in 30 minutes. Both updates landed cleanly
  inside the same PRs as the code changes that motivated them. The
  cost of "ADR for any durable architectural decision" is real but
  bounded — and the payoff is that future readers (humans / AI)
  inherit the why, not just the what.
