# 2026-05-10 — Measurement Day

> Single long session. Goal-of-the-day articulated mid-session: "make
> actual statistics work — restart Claude after this and run a task to
> see real measurement land." Five closed loops + one design epic +
> one process correction got there by session end. A second short
> afternoon arc then split Epic 6's scope (own-evidence vs general-
> user UX), spawned Epic 7 as a memo, opened T6.8 to retire
> `begin_task.sh`, and ran T6.7 live — banner / emit / render all
> green on a single librarian smoke dispatch.

## Done

- **T0.3 (#21) — FastAPI skeleton + `/health` + `/version`**. First
  closed loop of the day. Full fleet exercise: librarian × 2 against
  design 12 §HTTP server + ADR-0001; coder × 1 (deepseek-v4-pro,
  68.98s, 3675 tokens, only banner-captured number from before T6.1);
  reviewer × 1 (shadow, verdict pass / no findings); scribe × 1
  (shadow, picked verbatim by user). Then a **scribe-prompt
  enrichment experiment**: re-dispatched scribe with (a) librarian
  hard-constraint quotes verbatim, (b) measured verification facts
  (port, test counts, smoke responses), (c) explicit anti-patterns
  ("no speculative attribution", "no generic phrases"). Worker output
  matched and arguably exceeded orchestrator shadow on density and
  honesty; user picked the enriched version. Closed `gh issue close
  21` with comment summarising the run; merge `e10efce`, push
  `e10efce..73900e3` arc.

- **Epic 6 (#56) designed and decomposed** — "project effectiveness
  measurement & public landing page". Full design at
  `docs/design/56-effectiveness-page.md` (335 lines). Captures:
  append-only JSONL telemetry, deterministic Markdown renderer,
  honest methodology with "estimate vs measured" honesty key,
  `MAESTRO_DISPATCH_LOG` env-var path control, per-task + per-role
  aggregates, historical backfill plan. Decisions D1–D9 baked into
  the parent issue body so future-Opus reading the epic cold has the
  context.

- **T6.1 (#57) — worker banner parity** for librarian / reviewer /
  scribe. Surfaced design-vs-reality conflict during impl-start
  protocol: prefixing a banner string before strict-JSON output
  would break `json.loads()` for every JSON consumer (orchestrator
  + any external MCP client). Resolved by embedding banner as a
  `_banner` field inside the JSON object instead of as a prefix;
  coder keeps plaintext-prefix because its output is intentionally
  unstructured. `_build_banner()` + `extract_banner()` helpers added
  to `bootstrap/maestro_server.py`. 8 new tests (4 extract-banner +
  4 worker parametrised) + 3 existing tests updated to pop `_banner`
  before equality. Validators already document "Extra fields are
  ignored (forward compatibility)" so no allowlist edit needed.
  Commit `aae8098`, merge `c7cfad2`.

- **T6.2 (#58) — JSONL telemetry emit + env var + begin_task.sh**.
  Big task. `_emit_dispatch_row()` helper called from each of 4
  worker impls right after the existing `log_dispatch`. Schema per
  design 56 §2.1: `row_id`, `task_id`, `issue_number`, `tool`,
  `model`, `model_provider`, `wall_s`, `prompt/completion/total_tokens`,
  `started_at`, `journal_ref`, `is_estimate`, `est_method`,
  `supersedes`, `schema_version=1`, `error`. `MAESTRO_DISPATCH_LOG`
  three-mode control (unset → default path, empty → disabled,
  non-empty → that path). `scripts/begin_task.sh T<id> <issue>`
  exports `MAESTRO_CURRENT_TASK` / `MAESTRO_CURRENT_ISSUE`.
  `tests/conftest.py` autouse fixture redirects telemetry to
  tmp_path so existing 91 + new 11 tests don't pollute the real
  log. **Coder dispatch wall = 383.77s, 15114 tokens** — far past
  the nominal `REQUEST_TIMEOUT_SEC=120`, exposing a latent bug
  (the timeout constant is read but never wraps the API call;
  there's no `asyncio.wait_for`). Out of scope here, watchpoint
  carried forward. Commit `ea0aef2`, merge `2602397`.

- **T6.3 (#59) — `scripts/render_savings.py` + first `docs/savings.md`**.
  Pure read → aggregate → write. Per-task table (Task, Closed,
  Issue, Dispatches, Tokens, Wall, Est. Opus $, Worker $, Saved,
  Status), per-role aggregate (excludes is_estimate=true rows with
  footnoted count), headline paragraph, methodology link. Determinism
  rules enforced: sort orders stable, last-updated = max(started_at)
  not now(), provider rates baked as versioned constant
  (snapshot 2026-05-10), 50/50 prompt/completion split when only
  total_tokens available. 11 unit tests covering empty/blank/
  comments/malformed/per-task/per-role-exclude/unknown-model/null-
  tokens/determinism/unattributed-bucket/atomic-write. **Coder
  dispatch wall = 478.21s, 17045 tokens** — same latent timeout
  pattern. First placeholder `docs/savings.md` committed; populates
  on first measured dispatch after MCP restart. Commit `e799c4c`,
  merge `73900e3`.

- **Epic 6 sub-issue graph corrected**. Initially I created
  T6.2–T6.7 (#58–#63) as standalone issues with text-only "[Epic 6]"
  labelling and a checkbox list in the parent body — violating the
  project's actual convention (verified via GraphQL: Epic 5 #52 has
  `subIssues.totalCount = 3`). User caught it; I diagnosed root
  cause; user manually attached the 7 sub-issues to #56 via the
  GitHub sub-issues API. New memory file
  `feedback_epic_sub_issue_graph.md` records the rule for future
  sessions: title convention is necessary but not sufficient; the
  native graph link must be set explicitly via
  `POST /repos/.../issues/<epic>/sub_issues`.

- **Epic 6 scope split + T6.8 born + Epic 7 memo'd** (afternoon
  arc). Trying to start the T6.7 verification ritual exposed that
  `source scripts/begin_task.sh` only sets env vars in the caller's
  shell, not in the MCP server process — so for a fresh session
  it requires the user to source-then-restart-Claude-Code, which
  for shipped users is unacceptable friction. User's framing was
  decisive: "我把别人的工作模式当成了和我一样" — the env-var design
  silently assumed everyone organises by GitHub issues and is
  willing to source a shell script per task. Resolution: Epic 6
  stays scoped to *maestro the project's own savings evidence
  page*; general-user UX (Web UI savings page) splits out as
  Epic 7 (design pending, memory'd in
  `project_epic7_general_user_savings.md`); T6.8 (#64) added to
  Epic 6 to retire `begin_task.sh` in favour of dispatch
  parameters with git-branch fallback. Epic 6 #56 body amended:
  scope-clarification banner at top, D10 added to decisions,
  T6.8 added to sub-issues list, DoD count 7 → 8. T6.8
  attached to #56 via sub-issues API per
  `feedback_epic_sub_issue_graph.md` rule. New feedback memory
  `feedback_distinguish_user_from_general.md`: design must
  enumerate dogfooding-needs vs shipped-user-needs separately,
  never conflate.

- **T6.7 (#59→63) verified live, path B**. Skipped path A
  (`source begin_task.sh` + restart) since that helper is being
  retired by T6.8; path B exercises the load-bearing surfaces
  without binding the verification to soon-deprecated wiring.
  Single librarian dispatch against `docs/architecture.md`:
  deepseek-v4-flash, 15.34s, 2,803 tokens (1,908 prompt + 895
  completion). All four checks green — (1) JSON result carries
  `_banner` field with correct format, numbers matching the
  measured wall + tokens; (2) `docs/data/dispatch-log.jsonl`
  (gitignored, local) gained one row with full schema-v1 fields
  populated, `is_estimate=false`, `task_id` / `issue_number`
  null as expected (the `noattr` token in `row_id` makes the
  unattributed status legible at a glance); (3) `python
  scripts/render_savings.py` regenerated `docs/savings.md`
  cleanly — per-task table showed the dispatch as `—`, per-role
  showed Librarian: 1 / 2,803 / 15.3s / $0.10 estimated Opus
  baseline, `Last updated` field used `max(started_at)` not
  `now()` (determinism honoured); (4) the rendered file was
  reverted to the T6.3 placeholder afterwards — real
  `docs/savings.md` content lands progressively as T6.4 / T6.5 /
  T6.6 ship. T6.7 closed with full evidence comment;
  Epic 6 now 4/8.

## Decided

- **Worker scribe-prompt enrichment is the project's standing
  pattern**, not a one-off T0.3 experiment. Three things go into
  every scribe `convention` field from now on:
  1. Librarian-extracted hard-constraint quotes verbatim (so scribe
     has legal evidence to cite, not material to invent).
  2. Measured verification facts (test counts, smoke responses,
     real ports — not generated by the scribe).
  3. Explicit anti-patterns: "no speculative attribution", "no
     generic phrases like 'sets up foundation'". Forbidden vocabulary
     is more effective than positive guidance.
  Today's data point: with the enriched prompt, worker scribe
  output was indistinguishable from orchestrator-shadow output and
  was actually **safer** in one respect (explicitly marking the
  single-source-of-truth decision as "internal — not attributed to
  any ADR", which my orchestrator shadow had only conveyed
  implicitly). The "shadow gap" is mostly a context-injection gap,
  not a model-capability gap.

- **Telemetry control = configurable path, not master toggle**.
  User initially raised the perf-toggle question. Bench-of-back-of-
  envelope: JSONL append is ~1ms per dispatch; worker walls are
  60–500s. Overhead is in the noise — three orders of magnitude
  below the dispatch itself. Privacy / test-isolation are the real
  reasons for control, and `MAESTRO_DISPATCH_LOG` (empty=disable,
  non-empty=redirect, unset=default) covers all three cases more
  flexibly than a boolean. Recorded as D6 in #56's body.

- **Three open questions (OQ1/2/3) on Epic 6 design were
  user-delegated** ("依你"). Resolutions: (a) helper script
  `scripts/begin_task.sh` rather than CLAUDE.md behavioural mandate
  — lighter, recoverable when humans run workers directly. (b) T6.6
  (README link) lands immediately after T6.3 produces the first
  rendered page, not at end of epic. (c) Renderer-determinism CI
  verification deferred to v0.0.4 follow-up. Recorded as D7/D8/D9
  in #56's body.

- **Banner format asymmetry is the right design**, not a wart to
  paper over. Coder produces plaintext (orchestrator reads code,
  not JSON); the other three produce strict JSON. Banner shape is
  identical (`[<tool> dispatch — <model> — <wall>s — <tokens>
  tokens]`); placement varies by output type (plain-prefix vs
  `_banner` field). `extract_banner()` reads either uniformly. This
  preserves both contracts cleanly rather than retrofitting one
  worker to the other's convention.

- **Epic 6 is maestro's own evidence page; general users get a
  separate Epic 7** (afternoon decision; D10 in #56's body). The
  morning's design 56 quietly assumed `task_id` was always
  present because *the user* organises by GitHub issues —
  conflating "what maestro the project itself needs to publish"
  with "what shipped users will do". Concretely: per-task
  aggregation only makes sense for users with task concepts;
  per-time / per-role aggregates work universally. So Epic 6
  keeps the per-task savings page for maestro itself; Epic 7
  (Web UI page, design pending) handles the universal case for
  shipped users with per-task as opt-in. Memory'd in
  `project_epic7_general_user_savings.md`. The lesson is bigger
  than this one feature — recorded as feedback in
  `feedback_distinguish_user_from_general.md`.

- **D7 superseded by T6.8** (afternoon). Morning's resolution
  (`begin_task.sh` over a CLAUDE.md mandate) was correct *given*
  the framing "task_id must come from outside the MCP server".
  That framing was the unexamined assumption. T6.8 changes the
  framing: task_id flows in as an explicit dispatch parameter
  (orchestrator already knows the issue per implementation-start
  protocol step 1), with git-branch inference as a fallback for
  ad-hoc dispatches. `begin_task.sh` deprecated (warns) and
  removed in v0.0.4. Marked as superseded inline in #56's D7.

## Deferred

- **Latent async-dispatch timeout bug** in
  `bootstrap/maestro_server.py`. `REQUEST_TIMEOUT_SEC = 120` is
  read but never wraps the actual API call — there's no
  `asyncio.wait_for(..., timeout=...)`. Today's two long coder
  dispatches (383s, 478s) completed cleanly because DeepSeek
  responded eventually, but if it ever truly hangs, the dispatch
  hangs forever and the orchestrator polls a stuck job. Watchpoint
  for v0.0.3 ship. Not in scope for Epic 6.

- **T6.4 methodology page + ADR-0010** — open. Renderer already
  links to the file at relative path; link will go live when the
  page lands.

- **T6.5 backfill of historical 5 closed tasks** — open. Without
  this, `docs/savings.md` only shows tasks closed after T6.2's
  emit hook landed (i.e., starting from T6.7 / T0.4 / whatever
  comes next).

- **T6.6 README link** — open. Per OQ2 resolution it lands
  immediately after T6.3, but I held it back for a separate
  approval cycle since `README.md` is H3-protected.

- **T6.8 retire `begin_task.sh`** (#64, opened afternoon).
  Replaces env-var path with optional `task_id` / `issue_number`
  dispatch parameters (precedence: explicit param > env var
  deprecated > git-branch inference > unattributed). Touches all
  four worker tools' schemas + CLAUDE.md implementation protocol +
  design 56 §2.x. Independent of T6.4/T6.5/T6.6 — can land any
  time before v0.0.3 ship.

- **Epic 7 design + implementation** (memory'd, no issue yet).
  General-user savings UX in Web UI; per-time/per-role default;
  per-task opt-in. Single new session intended for design work —
  see `project_epic7_general_user_savings.md`. Implementation
  scheduling deferred (probably v0.0.4).

## Handoff for next session

- **Branch state**: on `v0.0.3`, fully pushed to `origin/v0.0.3`.
  Working tree clean. Local feature branches retained but already
  merged: `feature/21-webui-skeleton`,
  `feature/57-banner-parity`, `feature/58-jsonl-telemetry`,
  `feature/59-render-savings`, `docs/56-effectiveness-design`,
  `docs/journal-2026-05-10` (this entry). Safe to delete locally
  for clean `git branch` output; no remote presence either way.

- **Open issues at session end**: tracking (#2, #3); v0.0.3 epics
  (#11–#16, #52, #56); v0.0.3 sub-issues (#22–#51, #60–#62, #64).
  **Closed today**: #21 (T0.3), #57 (T6.1), #58 (T6.2), #59 (T6.3),
  #63 (T6.7). Epic 6 progress: **4/8 done** (afternoon arc added
  T6.8 to total).

- **Next implementation candidates** (pick one):
  1. **T6.6 (#60)** — README link. Smallest in the epic (~15 min).
     Best done immediately so the link goes live as soon as
     content exists.
  2. **T6.4 (#61)** — methodology page + ADR-0010. Docs-only
     (~1h). Unblocks reposting the wiki article with credible
     measurement claims.
  3. **T6.8 (#64)** — `begin_task.sh` retirement. ~1.5h. Touches
     four worker schemas + CLAUDE.md + design 56. Worth doing
     soon so subsequent dogfooded dispatches get attributed
     properly without needing the shell helper.
  4. **T0.4 (#22)** — empty-shell page + vendored htmx. Resumes
     Epic 0 forward progress; will exercise T6.8's parameter
     wiring as a side effect if that ships first.
  5. **T6.5 (#62)** — historical backfill (~1.5h). Can land
     after methodology page so the ⚠ symbol is defined for the
     reader.

- **Mental thread**: Epic 6 is now 4/8 with the load-bearing
  pieces (telemetry + renderer) shipped *and* live-verified.
  Remaining 4 are documentation + backfill + README link + the
  `begin_task.sh` retirement. None code-heavy except T6.8.
  The story arc has shifted: morning was "build the pipeline";
  afternoon was "discover the pipeline's external contract is
  user-hostile and design the fix". Tomorrow is whichever of
  T6.4 / T6.6 / T6.8 lands first.

- **Watchpoints carried forward**:
  - **Latent async-dispatch timeout** (above) — worth a fix
    sometime before v0.0.3 ship.
  - **Coder produces ~3 minor API mismatches per big-spec
    dispatch** in the concerns channel. T6.2 had 3 (function
    names, attribute names, async pattern); T6.3 had 1 (timestamp
    format). Pattern: coder is excellent on logic, lossy on
    matching the host project's exact identifiers. Mitigation:
    spec must explicitly name the existing API surface
    (`server._foo_impl`, `server.deepseek.chat.completions`,
    etc.) and link to one or two example test files; orchestrator
    integration always reviews concerns + reads the actual file
    before splicing.
  - **Live banner emission verified** (afternoon, path B) —
    librarian dispatch carried `_banner` field with measured
    wall + tokens; JSONL row + renderer both consumed it
    correctly. No longer a watchpoint.

## Process learnings

- **"Wait, check the logs first" is a debugging primitive that
  the orchestrator should reach for before declaring a worker
  stuck**. Mid-T6.2 the coder dispatch had been "running" for
  ~10 minutes (well past the nominal 120s timeout), so I escalated
  to user with three remediation options including "I write the
  code myself". User said "Wait. 模型不会出问题，看下日志". Three
  shell calls later, the truth was visible: dispatch had completed
  in 383s, log_dispatch wrote the entry, but the async job
  tracker's `record.status = "done"` apparently lagged the polling
  by some interval. The fix wasn't to abandon — it was to check
  one more poll. Generalises: **before declaring a system stuck,
  check whether it's actually stuck or whether your visibility
  layer is stuck**. The two failure modes look identical from the
  outside but have completely different remediation.

- **Designing for "estimate vs measured" honesty pays compounding
  interest**. The Epic 6 design distinguishes *measured* fields
  (worker API responses captured at dispatch) from *estimated*
  fields (Opus baseline, 50/50 token splits, banner-gap-era
  backfills) in the schema (`is_estimate` + `est_method`). The
  rendered page reflects this in a column (✓ / ⚠) and a footnote
  excluding ⚠ rows from per-role averages. The discipline isn't
  ornamental — it's what lets a public claim (~56% saved on T0.2)
  age into a measured claim without retracting anything. The
  estimate-day version of the page would have aged poorly within
  weeks; the measure-from-T0.4 version doesn't have a half-life.

- **GitHub has parallel mechanisms with non-obvious overlap**.
  Title convention "T<N>.<M> [Epic <N>]:" is a *hint* readable by
  humans and by `gh issue list`. The native sub-issues API is the
  *graph relationship* readable by GitHub's own progress tracker
  and any tool walking the parent-child edges. Easy to do one and
  miss the other. Today's miss: I created Epic 6 sub-issues with
  only the textual hint; user caught that the epic's progress
  bar wasn't reflecting children's states. Diagnostic via
  GraphQL on Epic 5 confirmed the correct convention is *both*.
  Generalises: **when working with hierarchical structures in any
  collaborative system, distinguish "human-readable label" from
  "machine-readable edge" and verify both exist**. Memory file
  `feedback_epic_sub_issue_graph.md` records the rule + the
  GraphQL verification command for next time.

- **Coder dispatches on big specs are slow but trustworthy**.
  Today's two big coder calls: 383s (15114 tokens), 478s (17045
  tokens). Orders of magnitude over the small-spec wall (60–120s
  range from T0.3 / Epic 5). DeepSeek-v4-pro chews through long
  reasoning + multiple files but doesn't error out. Practical
  consequence: **for big specs, set the orchestrator's polling
  expectations in advance**. "I'll check at +90s, +5min, +10min
  in the meantime I work on X" is a real strategy; "poll every
  30s and panic at 5min" produces a worse outcome (today I
  almost did the second, and was wrong).

- **Surfacing uncertainty before acting is the orchestrator's
  highest-leverage habit**. Three moments today reinforced this:
  (1) Banner-parity AC said "prefix result_text" but JSON workers
  can't be prefixed without breaking parsers — surfaced before
  coding, design + #57 body amended, then coded. (2) Sub-issue
  graph missing — surfaced and diagnosed before the next epic
  cycle replicated the mistake. (3) Coder dispatch "stuck" —
  surfaced the dogfooding-blocker, didn't silently write the
  code myself, and the user's check-the-logs prompt found the
  truth in three commands. Each surface + pause cost seconds;
  silently choosing wrong would have cost edits-to-undo, an
  inconsistent epic structure, and a working dispatch
  abandoned. CLAUDE.md's H6 ("always surface uncertainty") is
  not a politeness norm — it's a tactical primitive with
  measurable ROI.

- **The orchestrator-as-final-integrator is non-trivial and
  worth defending**. Worker output today consistently had ~1–3
  minor API mismatches against the actual host project (coder
  guessed `_dispatch_coder_impl` instead of `_coder_impl`,
  `deepseek_client` instead of `deepseek`, `pytest-asyncio`
  instead of `asyncio.run`, JSONL `finished_at` instead of
  `wall_s`). These are exactly the mistakes someone working
  cold from a spec would make — they're not hallucinations,
  they're plausible guesses that don't match the actual codebase.
  The orchestrator catches them at integration time because the
  orchestrator has lived in the codebase for the session and
  knows the real names. **The "AI as integrator" role is worth
  ~10–20% of the diff but is what makes the other 80–90% of
  the diff (worker-generated) survive contact with the actual
  codebase**. This is a scope of work that AI alignment
  discussions often skip, but it's what makes worker dispatch
  economically viable on a real project.

- **One session, five closed loops**. Today's arc: T0.3 → Epic 6
  design → T6.1 → T6.2 → T6.3, plus the sub-issue graph fix.
  Each was a true closed loop (`main` runnable after every merge,
  tests green, issue closed with a comment). The compounding
  effect: by session end, Epic 6 is 3/7 and the load-bearing
  pieces are shipped, vs the alternative shape of "a lot of
  in-progress branches and partial PRs". The closed-loop
  discipline isn't paid back later — it's paid for during the
  session, and it leaves you cleaner at every checkpoint.

- **My workflow ≠ the product's user workflow** (afternoon's
  big lesson). Epic 6's design quietly assumed every dispatch
  has a GitHub issue ID because that's how I personally organise
  work. The `begin_task.sh` helper was the visible artifact of
  that assumption: it requires the user to source a shell script
  before each task and re-launch Claude Code. For me dogfooding
  this is mildly annoying. For a shipped user it would be
  unacceptable friction. The fix isn't just "remove the helper"
  — it's a design discipline: any user-facing feature must
  enumerate "for maestro the project itself" vs "for general
  users" as two short paragraphs in the design doc, even when
  they collapse to the same answer. Recorded in
  `feedback_distinguish_user_from_general.md`. Generalises far
  beyond Epic 6: every feature added between now and v0.1
  ship needs this two-audience check, because most features
  this project builds *will* leak personal-workflow assumptions
  unless the design explicitly catches them.

- **A short follow-up session can re-frame a long one's work
  without invalidating it**. Morning's 5-loop arc was correct
  *given* its assumptions; afternoon's 1-arc surfaced an
  assumption that would have bitten before v0.0.3 ship and
  fixed it cheap (one new issue + one body edit + 2 memories +
  a verified pipeline). The morning's commits don't need to be
  redone; T6.8 is additive cleanup. Lesson: when something
  feels "almost right but slightly off" in a delivered design,
  short clarifying sessions are better than retrofit PRs. Cost
  here: ~30 minutes of session time, no code revert.

- **End-to-end verification with one minimal dispatch is
  sufficient** when the load-bearing surfaces are discrete
  and observable. T6.7 didn't need to wait for T0.4 to be a
  "real implementation task"; one librarian smoke call (15s,
  2.8k tokens, ~$0.0001) exercised banner emission + JSONL
  emit hook + renderer in series and produced four distinct
  observable artifacts. The original handoff said "verify on
  T0.4 (or next chosen task)" which framed verification as
  expensive — bundled into a real task. Reframing it as cheap
  and standalone made it land in the same session. Lesson:
  when verification feels like it must wait for "the next
  real thing", check whether it can run on a tiny synthetic
  thing instead. Often it can.
